name: Vite Build and Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
      - name: Checkout Code (Full History)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # 이전 단계에서 추가한 중요한 옵션은 유지합니다.
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' 
          
      - name: Install Dependencies
        run: npm install
        
      # ==========================================================
      # 🚨 문제 해결: working-directory를 '.'으로 설정하여 
      # 이 명령이 항상 프로젝트의 루트 폴더에서 실행되도록 강제합니다.
      # ==========================================================
      - name: Build Project
        run: npm run build
        working-directory: . # <-- 이 줄이 핵심입니다.
        
      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './dist' 
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4